/* Edit this file after you deploy the Apps Script web app. */
window.HOTDESK_CONFIG = {
  // Deploy > New deployment > Web app, then paste the /exec URL here.
  apiUrl: 'PASTE_YOUR_APPS_SCRIPT_EXEC_URL_HERE',

  // Drop your floor plan at docs/assets/floorplan.png (or change this path).
  // If the image is missing the app falls back to the grouped list view.
  floorplan: 'assets/floorplan.svg',
};
